import type { AsyncStorageWrapper } from 'next/dist/server/async-storage/async-storage-wrapper';
import type { StaticGenerationStore } from 'next/dist/client/components/static-generation-async-storage';
import type { IncrementalCache } from 'next/dist/server/lib/incremental-cache';
export declare type StaticGenerationContext = {
    pathname: string;
    renderOpts: {
        originalPathname?: string;
        incrementalCache?: IncrementalCache;
        supportsDynamicHTML: boolean;
        isRevalidate?: boolean;
        isOnDemandRevalidate?: boolean;
        isBot?: boolean;
        nextExport?: boolean;
        fetchCache?: StaticGenerationStore['fetchCache'];
    };
};
export declare const StaticGenerationAsyncStorageWrapper: AsyncStorageWrapper<StaticGenerationStore, StaticGenerationContext>;
